---
name: audit-mcp
description: Security-audits a third-party MCP server repo before installation; use for "audit this MCP", "is this MCP safe", "check this repo before I install it".
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_SKILL_DIR}/library/audit-mcp/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

---
name: trello-pipeline
description: Trello-based agentic product pipeline turning an idea into a PRD via staged AI agents on a live board; use for "run the Trello pipeline", "idea to PRD on my board", "set up the TrelloAgents board".
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_SKILL_DIR}/library/trello-pipeline/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

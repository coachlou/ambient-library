---
name: meeting-nuggets
description: Turns meeting transcripts into newsletter-style topic recaps with research add-ons and optional Notion publishing; use for "recap this meeting", "session report", or any pasted VTT/call transcript.
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_SKILL_DIR}/library/meeting-nuggets/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

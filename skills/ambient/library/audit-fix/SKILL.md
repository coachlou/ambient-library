---
name: audit-fix
description: Adversarial six-lens audit (errors, omissions, oversights, blind spots, conflicts, duplications) of a plan or change, then root-cause fixes; use for "audit this", "pressure test", "poke holes", "what did we miss".
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_SKILL_DIR}/library/audit-fix/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

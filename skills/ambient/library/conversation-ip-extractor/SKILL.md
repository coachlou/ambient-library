---
name: conversation-ip-extractor
description: Extracts named frameworks, methodologies, and publishable IP from a long conversation or session transcript; use for "what did I figure out here", "find the framework in this".
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_SKILL_DIR}/library/conversation-ip-extractor/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

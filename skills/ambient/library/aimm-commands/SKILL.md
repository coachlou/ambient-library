---
name: aimm-commands
description: Library of 54 named AIMM prompt commands (skeptic, canon-lock, blind-spot-scanner, transcript-miner, ...); use for "/aimm:<name>", "run the skeptic", or any request naming an AIMM command.
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_SKILL_DIR}/library/aimm-commands/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

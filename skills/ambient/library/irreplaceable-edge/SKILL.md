---
name: irreplaceable-edge
description: Guided live discovery of a professional's irreplaceable competitive edge against AI-first competition; use for "find my edge", "what's my moat", "how do I compete with AI".
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_SKILL_DIR}/library/irreplaceable-edge/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

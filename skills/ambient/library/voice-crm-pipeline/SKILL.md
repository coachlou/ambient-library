---
name: voice-crm-pipeline
description: Builds a hands-free voice-to-database capture chain (Siri to Shortcut to GPT to Sheets); use for "I never update my CRM", "talk to my database", "voice-to-sheet workflow".
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_SKILL_DIR}/library/voice-crm-pipeline/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

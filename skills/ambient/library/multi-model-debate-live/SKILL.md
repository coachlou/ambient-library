---
name: multi-model-debate-live
description: Orchestrates a live human-steered Claude/Gemini/Codex debate against one shared markdown file; use for "three-pane terminal", "run the models against each other" — the live workflow, not the prompt protocol.
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_SKILL_DIR}/library/multi-model-debate-live/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

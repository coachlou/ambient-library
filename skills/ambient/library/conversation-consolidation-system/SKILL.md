---
name: conversation-consolidation-system
description: Builds a four-stage pipeline turning scattered Claude chat history into a searchable Obsidian external brain; use for "I can't find what I discussed with Claude", "second brain for my AI work".
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_SKILL_DIR}/library/conversation-consolidation-system/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

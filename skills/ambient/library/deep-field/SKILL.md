---
name: deep-field
description: Integrated insight system combining Eigenthinking, latent-space cartography, and wisdom-of-crowds validation; use for "run deep field", frontier-quality insight beyond default reasoning.
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_SKILL_DIR}/library/deep-field/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

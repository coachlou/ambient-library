---
name: transcript-to-content-pipeline
description: Turns a raw transcript or session notes into a full content package (article, LinkedIn variants, summary, audio script); use for "turn this into content", "write something from this transcript".
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_SKILL_DIR}/library/transcript-to-content-pipeline/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

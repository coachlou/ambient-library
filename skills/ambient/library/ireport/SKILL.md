---
name: ireport
description: Generates styled, interactive HTML research reports from a topic or notes; use for "interactive report", "HTML report on X", "deep research document".
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_SKILL_DIR}/library/ireport/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

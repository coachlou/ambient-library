---
name: eigenthinking
description: Nine-step framework turning tacit expertise into branded, publishable IP (UP to IP); use for "turn how I think into a framework", "package my methodology", "make my process teachable".
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_SKILL_DIR}/library/eigenthinking/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

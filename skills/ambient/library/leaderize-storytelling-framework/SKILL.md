---
name: leaderize-storytelling-framework
description: Selects and applies storytelling frameworks for Leaderize's mid-level-leader audience across content stages and live sessions; use for Leaderize content or session design.
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_SKILL_DIR}/library/leaderize-storytelling-framework/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

---
name: geo-authority-architect
description: Builds a GEO/AI-citation authority strategy and Canon/Frameworks/Diagnostics content architecture; use for "GEO strategy", "get cited by AI", "GEARS".
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_SKILL_DIR}/library/geo-authority-architect/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

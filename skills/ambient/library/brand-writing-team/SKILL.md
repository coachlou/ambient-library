---
name: brand-writing-team
description: Multi-role team for brand-building long-form content with structured intake, end to end; use for "help me write a post about X", "turn this idea into a piece" when the goal is personal/company brand.
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_SKILL_DIR}/library/brand-writing-team/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

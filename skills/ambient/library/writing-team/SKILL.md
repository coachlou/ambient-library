---
name: writing-team
description: Full research -> draft -> edit pipeline to a publish-ready piece; use for "write me an article on X end to end", "take this from idea to finished post". Use this when the user wants the whole pipeline, not a single stage.
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_SKILL_DIR}/library/writing-team/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

---
name: knowledge-base-architect
description: Guides design and deployment of a personal AI knowledge base from accumulated expertise assets; use for "talk to my own knowledge", "build a private ChatGPT on my material".
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_SKILL_DIR}/library/knowledge-base-architect/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

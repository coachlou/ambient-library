---
name: context-mgr
description: Installs the multi-agent .context/ continuity system into a project; use for "install context-mgr", "set up multi-agent context here", "scaffold .context/", "add the shared brain to this project".
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_PLUGIN_ROOT}/library/context-mgr/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

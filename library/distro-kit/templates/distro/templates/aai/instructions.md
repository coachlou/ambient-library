# Instructions for {{NAME}}

1. Read `.aai/identity.md` and `.aai/context.md`.
2. The {{CAP}} capability lives in `.ailib/{{CAP}}/` (shadowed by `.aai/skills/{{CAP}}/` if present). Read its `instructions.md` before acting.
3. Run it through `./{{LAUNCHER}}`. Never edit `.ailib/`.

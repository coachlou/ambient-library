---
name: {{CAP}}
description: {{PURPOSE}}; use for "install {{CAP}}", "update {{CAP}}", "run {{LAUNCHER}}"
---

Read `instructions.md` in this skill's directory and follow it.

When installed as a plugin, this file lives at `${CLAUDE_PLUGIN_ROOT}/library/{{CAP}}/`;
when vendored by ambient-folder it lives at `.ailib/{{CAP}}/` (or `.aai/skills/{{CAP}}/`
if the folder owner has shadowed it).

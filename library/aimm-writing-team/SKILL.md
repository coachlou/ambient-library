---
name: aimm-writing-team
description: Autonomous multi-role team (Researcher, Strategist, Writer, Editor, Publisher) for thought-leadership articles; use for explicit role commands like "Use Strategist mode" or a full AIMM article run.
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_PLUGIN_ROOT}/library/aimm-writing-team/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

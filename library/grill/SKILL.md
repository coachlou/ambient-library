---
name: grill
description: Converges on a decision or spec via one high-leverage question at a time with a decisions ledger, then executes; use for "grill me", "interview me", "ask me questions one at a time".
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_PLUGIN_ROOT}/library/grill/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

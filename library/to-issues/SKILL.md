---
name: to-issues
description: Breaks a plan, spec, or PRD into independently-grabbable tracer-bullet issues on the project's issue tracker; use for "turn this plan into issues", "break this PRD into tickets".
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_PLUGIN_ROOT}/library/to-issues/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

---
name: researcher
description: Gathers sources into a structured research dossier; use for "research this topic", "find sources on X", "compile background". NOT for writing or editing the piece.
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_PLUGIN_ROOT}/library/researcher/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

---
name: project-brief
description: Creates a one-page project overview; use for "write a brief", "summarize this project", "project overview", "what is this project".
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_PLUGIN_ROOT}/library/project-brief/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

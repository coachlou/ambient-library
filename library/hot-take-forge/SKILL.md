---
name: hot-take-forge
description: Hardens a contrarian claim by forcing it to survive repeated self-attack until it is simultaneously novel AND true; use for "give me a hot take on X", "pressure test this take", "is this angle actually original".
metadata:
  summary: Hardens a contrarian claim by forcing it to survive repeated self-attack until it is simultaneously novel AND true; use for "give me a hot take on X", "pressure test this take", "is this angle actually original".
  status: experimental
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_PLUGIN_ROOT}/library/hot-take-forge/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

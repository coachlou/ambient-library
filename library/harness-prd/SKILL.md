---
name: harness-prd
description: Interviews the user to design a stateful-agent-harness ProcessSpec (a state machine of agents, skills, gates, artifacts); use for "build an agent process for X", "spec out an agent pipeline". NOT for code work-breakdowns (wbs-prd).
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_PLUGIN_ROOT}/library/harness-prd/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

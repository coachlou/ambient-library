---
name: latent-cartographer
description: Multi-phase latent-space traversal that maps the probability landscape before answering; use for "/lsc", "run the cartographer", "don't give me the median answer".
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_PLUGIN_ROOT}/library/latent-cartographer/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

---
name: extract-codify-patterns
description: Packages a working session into a shareable look-over-my-shoulder artifact bundle; use for "package this session", "codify this session", "turn this conversation into a teaching resource".
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_PLUGIN_ROOT}/library/extract-codify-patterns/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

---
name: cognitive-operations
description: Applies the Lenses/Operations/Recipes structured-thinking system to complex problems and decisions; use for "think through", "what am I missing", "run a pre-mortem", "use mental models".
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_PLUGIN_ROOT}/library/cognitive-operations/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

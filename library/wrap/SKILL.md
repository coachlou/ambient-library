---
name: wrap
description: Full session close-out ritual — commit, export conversation, teaching narrative, handoff, memory harvest; use for "wrap up", "close out", "export this conversation". For a plain .context/ checkpoint, prefer session.
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_PLUGIN_ROOT}/library/wrap/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

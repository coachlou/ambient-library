---
name: paradigm-collision-engine
description: Generates non-obvious insight by colliding 400+ disciplinary paradigms via parallel sub-agents and an elimination tournament; use for "fresh angle on X", "non-obvious take", "multiple lenses".
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_PLUGIN_ROOT}/library/paradigm-collision-engine/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

---
name: voice-profile-trainer
description: Derives a reusable voice profile from authentic writing samples so AI output sounds like the user; use for "doesn't sound like me", "write in my voice", "set up my voice profile".
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_PLUGIN_ROOT}/library/voice-profile-trainer/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

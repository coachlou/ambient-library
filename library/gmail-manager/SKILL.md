---
name: gmail-manager
description: General Gmail management across accounts via the gmail MCP (search, read, send, label, archive, drafts); use for "send an email", "find that thread", or mailbox actions beyond triage.
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_PLUGIN_ROOT}/library/gmail-manager/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

---
name: gmail-triage
description: Two-phase Gmail inbox sweep via the gmail MCP — batch-archive junk, then triage real emails one at a time; use for "check my email", "clean up my inbox".
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_PLUGIN_ROOT}/library/gmail-triage/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

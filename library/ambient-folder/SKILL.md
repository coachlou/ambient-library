---
name: ambient-folder
description: Makes a folder ambiently intelligent and maintains it — interviews for purpose when it's unclear, writes the .aai/ scaffold, vendors canonical capabilities into .ailib/, forks them, and re-syncs; use for "make this folder ambient", "set up an agentic folder for X", "stamp .aai here", "interview me about this folder", "add a capability to this folder", "re-sync my vendored skills".
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_PLUGIN_ROOT}/library/ambient-folder/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

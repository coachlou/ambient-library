---
name: cognitive-stack-code
description: The right mental model for the problem you're stuck on, not a pile of frameworks. Routes a stuck problem to the right *kind* of thinking tool from a large library of mental models, runs a recipe, and returns a five-part answer: a plain answer, a mirror of your own thinking (the models, biases, and worldview in play), the reasoning trace, the recipe-driven answer, and an optional next move you can act on in Claude Code. Use when a decision, diagnosis, strategy call, or stuck problem needs the *right kind* of mental model, not a stack of frameworks.
metadata:
  summary: Routes a stuck problem to the right kind of mental model from a 2,375-model library, runs a recipe, and returns a five-part answer (plain answer, cognitive mirror, reasoning trace, recipe answer, optional next move); use for a decision, diagnosis, or stuck problem needing the right kind of model, not a pile of frameworks.
---

Read `instructions.md` in this skill's directory and follow it.

Path note: this skill also ships inside the `ambient` library plugin, so its
instructions may reference files as `${CLAUDE_PLUGIN_ROOT}/library/cognitive-stack-code/<file>`.
When installed standalone, resolve those to `<file>` in this directory.

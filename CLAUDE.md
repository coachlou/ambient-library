# ambient-library

**Read `AGENTS.md` in this folder, then `.aai/instructions.md`, and follow it.**

This folder is ambient — its behavior lives in `.aai/`. `AGENTS.md` holds the
canonical block; this file is a redirect so Claude Code (which auto-loads
`CLAUDE.md`, not `AGENTS.md`) lands in the same place. That split is this
library's own convention — see `templates/AGENTS-pointer.md`.
